import { ValidaterDirective } from './validater.directive';

describe('ValidaterDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidaterDirective();
    expect(directive).toBeTruthy();
  });
});
